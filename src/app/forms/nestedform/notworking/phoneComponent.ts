/*import {Component, View, SimpleChange, Input, Output, EventEmitter, OnInit} from '@angular/core';
import {bootstrap} from 'angular2/platform/browser';
import {FORM_DIRECTIVES, FormBuilder, Validators, ControlGroup, Control} from '@angular/common';

@Component({
    selector: 'phone',
    templateUrl: './phoneForm.html',
    directives: [FORM_DIRECTIVES],
    viewproviders:[FormBuilder]
})

export class PhoneComponent {
    phoneForm;

    constructor(fb: FormBuilder) {
      this.phoneForm = fb.group({
        "phone":["", Validators.required]
      })
    }
}*/