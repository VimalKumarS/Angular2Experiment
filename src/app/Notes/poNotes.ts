import {Component, Input, AfterContentInit} from '@angular/core'

@Component({moduleId: module.id, selector: 'po-Notes', templateUrl: 'poNotes.html', styleUrls: ['poNotes.css']})
export class poNotes {

    constructor()
    {}
}